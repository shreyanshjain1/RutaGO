{
  "name": "rutago-mobile-wrapper",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper for RutaGO MVP delivery on Android and iOS",
  "scripts": {
    "sync": "cap sync",
    "copy": "cap copy",
    "add:android": "cap add android",
    "add:ios": "cap add ios",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "build:android": "cap sync android",
    "build:ios": "cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  }
}